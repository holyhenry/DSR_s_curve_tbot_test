7/11 test:
    GOOD
    1. Using 2 low pass is a MUST (decent gain value 0.01 - 0.1)
7/18 test:
    GOOD
    1. Increasing tau SIGNIFICANTLY decrease spacing error 0.02 -> 0.01 (or 0.008)
    BAD
    2. Potential lighting issue, robot not catching up at very specific location

10/7 test:
    1. tbot162 has setup quick system start. Run the following command for example:
        $ roslaunch tbot_launch system_bringup.launch follower_indx:=0 spacing:=0.25 controller_mode:=DSR tau:=0.008

10/16 setup mocap marker:
    1. to get rostopic of rigid object pose:
        $ roslaunch vrpn_client_ros sample.launch server:=192.168.0.175

10/24 varying time system (instead of fix tau) - work plan: 
    1. odom (self state memory)
        modify TrackingNode::odomCallback()
    2. camera (predecessor state memory)
        modify TrackingNode::tagCallback() and TrackingNode::aprilTagFilter()
        then, modify Controller::setObservations() and Controller::getTarget()
        (add time stamps array along side with observations_)